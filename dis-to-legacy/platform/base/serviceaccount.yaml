# Linkerd derives workload identity from the ServiceAccount. The default SA
# would make every pod in the namespace indistinguishable and the authorization
# policy meaningless, so each workload gets its own, giving it a nameable
# identity of the form <sa>.<namespace>.serviceaccount.identity.linkerd.cluster.local.
apiVersion: v1
kind: ServiceAccount
metadata:
  name: proxy
